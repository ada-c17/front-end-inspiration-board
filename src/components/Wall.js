import React, { useState } from "react";
import "./Wall.css";
import Board from "./Board";

const Wall = () => {
  const [display, setDisplay] = useState([]);

  // some logic that says if useState is default then return BoardButtons
  // else:
  //   return
  //   <Board board_id = {display}></Board>
};

export default Wall;
